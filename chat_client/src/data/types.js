// data object schema

// TODO: Add Typescript to make it a type definition interface

const ChatQuery = {
  Chat: {
    id: 'string',
    sender: 'string',
    message: 'string'
  }
}

export default ChatQuery;
