// data object schema

// TODO: Add Typescript to make it a type definition interface

const Chat = {
  id: 'string',
  from: 'string',
  createdAt: 'number',
  content: 'string'
}

export default Chat;
